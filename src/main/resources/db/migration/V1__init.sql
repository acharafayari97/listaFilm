CREATE TABLE FILM (
  ID        NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  TITOLO    VARCHAR2(255 CHAR)      NOT NULL,
  REGISTA   VARCHAR2(255 CHAR)      NOT NULL,
  ANNO      NUMBER(4)               NOT NULL,
  VOTO      NUMBER(3)               NOT NULL
);